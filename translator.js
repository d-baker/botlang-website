$(document).ready(function() {
    // TODO why is this just refreshing the page?
    var sentence = $("textarea").val();
    $("button").click(function() {
            console.log('test');
            console.log(sentence);
        });

    // get json 
});
